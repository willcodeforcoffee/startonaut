<% content_for :title, "Editing tag" %>
<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing tag</h1>
  <%= render "form", tag: @tag %>
  <%= default_link_to "Show this tag", @tag, class: "inline-block" %>
  <%= default_link_to "Back to tags", tags_path, class: "inline-block" %>
</div>
