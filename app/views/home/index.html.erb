<div>
  <h1 class="font-bold text-4xl">Startonaut</h1>
  <p>Welcome to a new start page for Internauts.</p>
  <p>You are <%= authenticated? ? "authenticated" : "not authenticated" %></p>
  <ul>
    <li><%= link_to "Login", new_session_path unless authenticated? %></li>
    <li><%= link_to "Logout", session_path, method: :delete, data: { "turbo-method": :delete } if authenticated? %></li>
  </ul>
</div>
